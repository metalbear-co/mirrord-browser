Fixed the header-observation canary reporting users as blocked when header injection was working. The canary was armed in the popup while the header is observed in the service worker, so the success signal could never reach it, and service worker analytics were dropped entirely because `capture` read `localStorage`, which does not exist there. The canary is now armed and resolved in the service worker, and it reports how much traffic it saw and whether the rule was scoped.
